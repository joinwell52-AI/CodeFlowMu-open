# CodeFlowMu projects

`projects/` stores multiple independent CodeFlowMu team projects.
Each project owns its `fcop/` collaboration ledger and may use an internal `workspace/` for business artifacts in multi-product mode.
Legacy install-local projects under `workspace/` remain supported in place and are never moved automatically.
